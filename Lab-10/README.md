# Лабораторна робота 10: Обходи графів

## [IDE](https://onecompiler.com/cpp)

---

## Завдання 1: Пошук в ширину (BFS)

### Мета
Реалізувати алгоритм пошуку в ширину (Breadth-First Search) для обходу графа. Навчитись працювати з графами, представленими списком суміжності, та використовувати чергу для обходу.

### Вимоги

Створіть програму, яка:
- Приймає граф, представлений списком суміжності
- Реалізує функцію `bfs` для обходу графа в ширину від заданої вершини
- Виводить порядок відвідування вершин

**Бали:** 2

**Складність:** O(V + E), де V - кількість вершин, E - кількість ребер

### Приклад `main`

```cpp
#include <iostream>
#include <vector>
#include <queue>
#include <unordered_set>
using namespace std;

// Функція для BFS обходу графа
void bfs(int start, const vector<vector<int>>& graph) {
    // Ваш код тут
}

int main() {
    // Приклад графа (список суміжності)
    // Граф:
    //     0 --- 1
    //     |     |
    //     2 --- 3 --- 4
    vector<vector<int>> graph = {
        {1, 2},    // вершина 0 з'єднана з 1 та 2
        {0, 3},    // вершина 1 з'єднана з 0 та 3
        {0, 3},    // вершина 2 з'єднана з 0 та 3
        {1, 2, 4}, // вершина 3 з'єднана з 1, 2 та 4
        {3}        // вершина 4 з'єднана з 3
    };

    cout << "BFS обхід від вершини 0: ";
    bfs(0, graph);
    cout << endl;

    cout << "BFS обхід від вершини 2: ";
    bfs(2, graph);
    cout << endl;

    return 0;
}
```

### Приклад запуску

```text
BFS обхід від вершини 0: 0 1 2 3 4
BFS обхід від вершини 2: 2 0 3 1 4
```

---

## Завдання 2: Пошук в глибину (DFS)

### Мета
Реалізувати алгоритм пошуку в глибину (Depth-First Search) для обходу графа. Навчитись використовувати рекурсію або стек для обходу графа в глибину.

### Вимоги

Створіть програму, яка:
- Приймає граф, представлений списком суміжності
- Реалізує функцію `dfs` для обходу графа в глибину від заданої вершини
- Виводить порядок відвідування вершин
- Реалізує як рекурсивну версію

**Бали:** 2

**Складність:** O(V + E), де V - кількість вершин, E - кількість ребер

### Приклад `main`

```cpp
#include <iostream>
#include <vector>
#include <stack>
#include <unordered_set>
using namespace std;

// Рекурсивна версія DFS
void dfsRecursive(int vertex, const vector<vector<int>>& graph,
                  unordered_set<int>& visited) {
    // Ваш код тут
}

// Обгортка для рекурсивного DFS
void dfs(int start, const vector<vector<int>>& graph) {
    unordered_set<int> visited;
    dfsRecursive(start, graph, visited);
}

// Ітеративна версія DFS (з використанням стеку)
void dfsIterative(int start, const vector<vector<int>>& graph) {
    // Ваш код тут
}

int main() {
    // Приклад графа (список суміжності)
    // Граф:
    //     0 --- 1
    //     |     |
    //     2 --- 3 --- 4
    vector<vector<int>> graph = {
        {1, 2},    // вершина 0 з'єднана з 1 та 2
        {0, 3},    // вершина 1 з'єднана з 0 та 3
        {0, 3},    // вершина 2 з'єднана з 0 та 3
        {1, 2, 4}, // вершина 3 з'єднана з 1, 2 та 4
        {3}        // вершина 4 з'єднана з 3
    };

    cout << "DFS обхід (рекурсивний) від вершини 0: ";
    dfs(0, graph);
    cout << endl;

    cout << "DFS обхід (ітеративний) від вершини 0: ";
    dfsIterative(0, graph);
    cout << endl;

    cout << "DFS обхід (рекурсивний) від вершини 2: ";
    dfs(2, graph);
    cout << endl;

    return 0;
}
```

### Приклад запуску

```text
DFS обхід (рекурсивний) від вершини 0: 0 1 3 2 4
DFS обхід (ітеративний) від вершини 0: 0 2 3 4 1
DFS обхід (рекурсивний) від вершини 2: 2 0 1 3 4
```

> **Примітка:** Порядок обходу в DFS може відрізнятися залежно від того, в якому порядку додаються сусідні вершини. Ітеративна версія може давати інший порядок, ніж рекурсивна.
 ---

## Сумарні бали

- **Завдання 1 (BFS):** 2 бали
- **Завдання 2 (DFS):** 2 бали

**Всього:** 4 бали
